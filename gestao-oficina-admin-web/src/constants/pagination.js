/** Tamanho de página padrão do sistema (server-side e client-side). */
export const DEFAULT_PAGE_SIZE = 20;

/** Máximo permitido pelo backend (JdbcMappingUtils.clampPageSize). */
export const MAX_PAGE_SIZE = 100;
